import Homey from 'homey';

class GlueApp extends Homey.App {

  /**
   * onInit is called when the app is initialized.
   */
  async onInit() {
    this.log('GlueLock has been initialized');
    
    
  }

}

module.exports = GlueApp;
